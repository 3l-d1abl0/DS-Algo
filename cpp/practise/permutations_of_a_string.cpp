#include<bits/stdc+.h>

using namespace std;


void permute(char *str, int i, int n){

    if(i==n)
        printf("%s\n",)

}

int main(){

    char str[] = "ABC";
    int n = strlen(str);
    permute(str, 0, n-1);

    return 0;
}
